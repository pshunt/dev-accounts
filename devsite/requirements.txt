# Core Framework
Django>=5.0,<6.0

# Database (PostgreSQL support — comment out if using SQLite)
psycopg2-binary>=2.9

# Environment Variables (for production secrets)
python-decouple>=3.8

# Static file handling for production
whitenoise>=6.6

# Optional utilities
gunicorn>=21.2  # WSGI server for Render/Heroku-style deployment

# For password reset email links (if using email backend)
django-allauth>=0.63.3  # optional: adds robust auth features

# Optional if you ever add image uploads or avatars
Pillow>=10.0